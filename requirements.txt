# ── GUI 框架 ──
PySide6>=6.6.0

# ── PDF 处理 ──
pymupdf>=1.24.0
pymupdf4llm>=0.0.5

# ── 文档格式 ──
mammoth>=1.6.0
python-pptx>=0.6.21
EbookLib>=0.18

# ── HTML / 文本 ──
beautifulsoup4>=4.12.0
markdownify>=0.11.0
chardet>=5.0
lxml>=4.9.0

# ── OCR 支持（扫描件 PDF）──
rapidocr-onnxruntime>=1.3.0

# ── LLM 增强（可选，V2+ 启用）──
# openai>=1.0
